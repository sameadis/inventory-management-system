# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service Role Key (Required for admin operations like inviting users)
# ⚠️ NEVER expose this key to the client/browser - only use in server-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# External Auth App URL (ALIC-Calendar)
# In production, point this to the shared domain auth route, e.g.:
# NEXT_PUBLIC_AUTH_URL=https://yourdomain.com/auth

# Calendar app base URL used by Next.js rewrites to proxy calendar routes
# In production, this should be the Vercel URL of the calendar app, e.g.:
# CALENDAR_APP_URL=https://alic-calendar.vercel.app

